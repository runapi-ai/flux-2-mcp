{
  "mcpServers": {
    "flux-2": {
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/flux-2-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
